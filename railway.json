{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "NPM_CONFIG_PRODUCTION=false npm install --include=dev --no-audit --no-fund && npm --workspace @gta-rp/shared run build && npm --workspace @gta-rp/server run build"
  },
  "deploy": {
    "startCommand": "npm --workspace @gta-rp/server run start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30
  },
  "plugins": [
    { "plugin": "postgresql", "options": { "version": "16" } }
  ]
}
