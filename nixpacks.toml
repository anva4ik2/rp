# Pins Node.js + npm versions for Railway / Nixpacks. Node 20 LTS includes
# stable fetch() and AbortController, both used by our ApiClient.
[phases.setup]
nixPkgs = ["nodejs_20", "npm-9_x"]

[variables]
NPM_CONFIG_PRODUCTION = "false"
